@model Domain.Entity.Cargos

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Cargos</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.cod_cargo)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.cod_cargo)
            @Html.ValidationMessageFor(model => model.cod_cargo)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.des_cargo)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.des_cargo)
            @Html.ValidationMessageFor(model => model.des_cargo)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
